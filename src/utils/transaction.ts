// import { CardsService } from 'src/cards/cards.service';
// import { Transaction } from 'src/entities';

// export const createTransaction = (transaction: Transaction) => {
//   const { card_id } = transaction;
//   const card = new CardsService().findOne(Number(card_id));
//   console.log(card);
// };
