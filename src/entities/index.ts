import { Card } from './card.entity';
import { Transaction } from './transaction.entity';
import { User } from './user.entity';

const entities = [User, Card, Transaction];

export { User, Card, Transaction };
export default entities;
